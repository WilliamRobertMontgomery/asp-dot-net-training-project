<authorization>
@if(Request.IsAuthenticated) {
    <text>Вы вошли как <strong>@User.Identity.Name</strong>
    [ @Html.ActionLink("Выход", "LogOff", "Account") ]
    

        
        @if(User.Identity.IsAuthenticated)
        {
            if(User.IsInRole("admin") || User.IsInRole("moderator"))
            {
                 <text> [ @Html.ActionLink("Админ панель", "Index", "Admin") ] </text>
            }
            
            <text> [ @Html.ActionLink("Профиль", "ShowProfile", "Profile") ] </text>
        }
        
    
    
    [ @Html.ActionLink("На главную", "Index", "Home") ]
    
    </text>
}
else
{
    @:[ @Html.ActionLink("Войти", "LogOn", "Account") ]
    @:[ @Html.ActionLink("Регистрация", "Register", "Account") ]
}
</authorization>
