@model Lab4.Models.AddBuilder

@{
    ViewBag.Title = "AddBuilder";
}

<h2>Добавить строителя</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Добавить строителя</legend>            
        

        <div class="editor-label">
            @Html.LabelFor(model => model.builderName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.builderName)
            @Html.ValidationMessageFor(model => model.builderName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
